import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <h2>Search Bar</h2>
      {/* Add search input and functionality here */}
    </div>
  );
};

export default SearchBar;
